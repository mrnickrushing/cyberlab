[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "celery -A worker worker --loglevel=info --concurrency=4"
healthcheckPath = ""
healthcheckTimeout = 0
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
